// ignore_for_file: constant_identifier_names

enum TaskCondition { coming, now, over }

enum ExamType { practice, exam, group }

enum CourseContentType {
  pdf,
  video,
  note,
  live,
  link,
  assignment,
  testmoj,
  exam,
  writtenExam,
}

enum VideoSourceType {
  youtube,
  embedded,
  vimeo,
}

enum OverviewType { classes, notes, exams }

enum CourseParams {
  contents,
  sections,
  categories,
  courseFeatures,
  courseRoutines,
  faqs,
  users,
  instalments,
  authors,
}

enum SettingsKeyType { app_version, app_name, app_update }

enum ApiFilters { free }

enum CommentableType { post, comment, content, notice, course }

enum PaymentMethod { bkash, amarPay }
